# Database Configuration
# Use either individual parameters:
user=your_db_user
password=your_db_password
host=your_db_host
port=5432
dbname=your_db_name

# OR use a connection URL (legacy support):
# SUPABASE_DATABASE_URL=postgresql://user:password@host:port/dbname?schema=public

# Docker Configuration
DATA_DIR=/app/data

# Optional: Logging
# LOG_LEVEL=INFO 